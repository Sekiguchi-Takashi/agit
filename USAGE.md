# 操作マニュアル

起動方法は [README.md](./README.md) を参照してください。
ここではアプリ起動後の使い方を説明します。

## 画面構成

- 上部: アプリ名と使用中のモデル名
- 右上「履歴クリア」ボタン
- 中央: 会話表示エリア（自分の発言は右側・青、AIの返答は左側）
- 下部: メッセージ入力欄と「送信」ボタン

## 基本操作

- **送信**: 入力欄に文字を入れて「送信」ボタンを押す、または Enter キー
  （改行したい場合は Shift+Enter）
- 送信中は入力欄と送信ボタンが自動で無効化されます
- AIの応答は届いた分だけリアルタイムに表示されます

## 会話履歴

- 会話は自動的に保存され、アプリを再起動しても続きから表示されます
- 「履歴クリア」ボタン →確認ダイアログで「OK」を押すと、これまでの会話が
  すべて削除されます（元に戻せません）

## 終了方法

- `node server.js` を実行しているTermuxセッションで `Ctrl + C`
- Ollamaも終了する場合は、`ollama serve` を実行しているセッションでも
  `Ctrl + C`

## 設定変更（起動時のみ指定可能）

```bash
OLLAMA_MODEL=qwen3:4b node server.js   # 使用モデルを変更
PORT=8080 node server.js               # アプリのポート番号を変更
```

## エラーが表示されたとき

| 画面の表示 | 対処 |
|---|---|
| Ollamaに接続できません | `ollama serve` のセッションが起動しているか確認 |
| サーバーに接続できません | `node server.js` が起動しているか確認 |
| メッセージが空です | 何か文字を入力してから送信してください |
